use flake

# Local credentials (.env, gitignored — see .env.example). Loaded here so a
# plain `cargo run` sees them, and so `cargo build` bakes the TONE3000 key
# into plugin binaries via option_env! — a DAW does not inherit this shell.
dotenv_if_exists .env
