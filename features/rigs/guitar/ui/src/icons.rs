//! Module/block glyphs — re-exported from the grid crate so every surface
//! (graph, perform tiles, tree badges) shares one icon language.

pub use signal_grid_ui::icons::{block_icon, module_icon};
