# Synth — Omnisphere

Behaviour spec for the Omnisphere routing experiment and `.prt_omn` / `.mlt_omn`
patch import. Requirements land here as the engine mapping is formalized; the
implementation lives in `features/synth/src/`.
