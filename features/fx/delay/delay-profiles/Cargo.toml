[package]
name = "delay-profiles"
version.workspace = true
authors.workspace = true
license.workspace = true
edition = "2021"          # kept on 2021 — pre-move signal workspace edition; migrate deliberately
description = "Delay hardware profiles — Space Echo, Echoplex parameter mappings"

[dependencies]
delay-dsp = { workspace = true }
eq-profiles = { workspace = true }
