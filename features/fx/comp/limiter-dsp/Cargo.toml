[package]
name = "limiter-dsp"
version.workspace = true
authors.workspace = true
license.workspace = true
edition.workspace = true
description = "Limiter processing engine — multi-stage clipper, waveshaper, mu-law, and slew-rate limiters"

[dependencies]
fts-dsp = { workspace = true }
