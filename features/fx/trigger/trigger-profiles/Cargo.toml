[package]
name = "trigger-profiles"
version.workspace = true
edition = "2021"          # matches sibling fx profiles crates (eq-profiles)
authors.workspace = true
license.workspace = true
description = "trigger-profiles — drum trigger profiles: full parametric control and simplified drum replacer modes (parameter mappings over trigger-dsp, no GUI)"

[dependencies]
trigger-dsp = { workspace = true }
