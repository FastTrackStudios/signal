[package]
name = "reverb-profiles"
version.workspace = true
authors.workspace = true
license.workspace = true
edition = "2021"          # kept on 2021 — pre-move signal workspace edition; migrate deliberately
description = "Reverb hardware profiles — EMT 140, Lexicon 480 parameter mappings"

[dependencies]
reverb-dsp = { path = "../reverb-dsp" }
eq-profiles = { workspace = true }
