[package]
name = "reverb-ui"
version.workspace = true
authors.workspace = true
license.workspace = true
edition = "2021"          # kept on 2021 — pre-move signal workspace edition; migrate deliberately
description = "Reverb GUI — decay visualization, wet/dry mix, and profile theme rendering"

[dependencies]
audio-gui = { workspace = true }
fts-plugin-core = { workspace = true }
reverb-dsp = { path = "../reverb-dsp" }
reverb-profiles = { path = "../reverb-profiles" }
