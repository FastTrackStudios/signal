[package]
name = "modulation"
version.workspace = true
edition = "2021"          # kept on 2021 — pre-move signal workspace edition; migrate deliberately
license.workspace = true
description = "modulation — facade over chorus/trem/wah DSP; UI + profiles opt-in."

[features]
default = []
ui = []
profiles = []

[dependencies]
chorus-dsp = { workspace = true }
trem-dsp = { workspace = true }
wah-dsp = { workspace = true }
