[package]
name = "signal-fx"
version.workspace = true
edition = "2021"          # kept on 2021 — pre-move signal workspace edition; migrate deliberately
license.workspace = true
description = "Built-in FX for signal — PluginInstance wrappers over the vendored FX DSP cores (eq-dsp, comp-dsp, …)."

[dependencies]
hound = "3.5"
signal-plugin-host = { workspace = true }
saturate-dsp = { workspace = true, features = ["analysis"] }
audiocore-dsp = { workspace = true }
# FX facades (never the -dsp crates directly).
eq = { workspace = true }
comp = { workspace = true }
reverb = { workspace = true }
delay = { workspace = true }
level-dsp = { workspace = true }
modulation = { workspace = true }

[dev-dependencies]
# Integration test: built-in FX through the standalone daw's Effects::add.
daw = { workspace = true, features = ["standalone"] }
