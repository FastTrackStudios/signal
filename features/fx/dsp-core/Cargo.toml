[package]
name = "dsp-core"
version.workspace = true
edition = "2024"
authors.workspace = true
license.workspace = true
description = "Shared primitives for the DSP cores — the audited numeric-conversion boundary and per-channel state. no_std, no dependencies, no allocation."

[lints]
workspace = true

[dependencies]
