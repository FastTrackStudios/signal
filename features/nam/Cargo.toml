[package]
name = "signal-nam"
version.workspace = true
edition = "2021"          # kept on 2021 — pre-move signal workspace edition; migrate deliberately
license.workspace = true
authors.workspace = true

[dependencies]
signal-proto.workspace = true
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true
# Header-only WAV probing for the IR scanner (no features: probe is always on).
fts-sample.workspace = true
sha2 = "0.10"
walkdir = "2"
# The similarity space + the model inference the prober drives (#77 M5).
signal-space.workspace = true
neural-amp-modeler.workspace = true
tracing.workspace = true
base64 = "0.22"
regex = "1"

[dev-dependencies]
tempfile = "3"
