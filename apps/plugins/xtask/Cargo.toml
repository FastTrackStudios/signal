[package]
name = "fts-plugin-xtask"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "Bundler for the FTS plugin suite — wraps nice-plug-xtask (`cargo run -p fts-plugin-xtask -- bundle <package> --release`)"

[[bin]]
name = "fts-plugin-xtask"
path = "src/main.rs"

[lints]
workspace = true

[dependencies]
nice-plug-xtask.workspace = true
