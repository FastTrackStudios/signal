# FastTrackStudio signal engine — supervised for live use.
# Installed by `just rig-install` to ~/.config/systemd/user/; the ONE
# fasttrackstudio binary lives in ~/.local/lib/fts/ (decoupled from the
# dev target/ dir so builds can't disturb the running service) and runs
# in headless engine mode; the web remote is embedded in the binary
# (feature embed-web).
#
#   systemctl --user status signal-engine     # health
#   journalctl --user -u signal-engine -f     # logs
#   just rig-install                           # build + deploy + restart

[Unit]
Description=FastTrackStudio signal engine (guitar rig core)
# PipeWire is a user service; the engine reconnects if it bounces.
After=pipewire.service
Wants=pipewire.service

[Service]
# No launcher prefix. This used to start through `pw-jack`, because the
# rig's hardware MIDI was midir on the pipewire-jack backend: without
# PipeWire's JACK shim on LD_LIBRARY_PATH, midir found no jackd, enumerated
# ZERO ports, and the rig silently never saw a keyboard. Audio was
# unaffected, which is what made the failure so confusing — the rig played,
# it just could not be played. MIDI is now midicore's native PipeWire
# backend, so the engine speaks to PipeWire directly.
# @LAUNCH@ is substituted with nothing by `just rig-install`; it is kept so
# an already-installed unit template still renders.
ExecStart=@LAUNCH@%h/.local/lib/fts/signal-desktop --engine
Environment=RUST_BACKTRACE=1
# OTLP telemetry (traces + logs + metrics) to the local collector
# (the Task observability stack: collector → Tempo/Loki, Grafana on top).
# The exporter speaks http/protobuf, so this is the collector's HTTP port
# (4318), NOT the gRPC one (4317). Service name: fts-engine.
# REMOVING this line silently disables ALL export — the engine runs fine
# but nothing lands in Grafana; console/journal logs are unaffected.
Environment=OTEL_EXPORTER_OTLP_ENDPOINT=http://127.0.0.1:4318
# Crash-only: whatever happens, be back in ~1s.
Restart=always
RestartSec=1
# A hung engine is worse than a dead one on a Sunday — if stop ever
# hangs, kill hard and let Restart bring it back.
TimeoutStopSec=5

[Install]
WantedBy=default.target
