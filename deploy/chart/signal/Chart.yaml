apiVersion: v2
name: signal
description: >-
  signal.fasttrackstudio.app — the Signal site: the landing page with its
  rig demos, the per-rig pages, and the guide. A static wasm bundle and
  nothing else; there is no server and no state.
type: application
home: https://github.com/FastTrackStudios/signal
sources:
  - https://github.com/FastTrackStudios/signal
# Chart version — bump on chart changes.
version: 0.1.0
# Tracks the workspace version ([workspace.package].version). The image
# tag defaults to this unless values override it, which in practice they
# always do — argocd-image-updater writes the digest it rolled.
appVersion: "0.1.0"
